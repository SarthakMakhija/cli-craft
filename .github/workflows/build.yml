name: cli-craft CI

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env: 
      ZIG_VERSION: "0.14.1" 
      SOURCE_DIR: "src"
      TEST_ROOT_SRC: "src/cli-craft.zig"
      BUILD_DIR: "zig-out"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Zig
        run: |
          wget https://ziglang.org/download/${{ env.ZIG_VERSION }}/zig-x86_64-linux-${{ env.ZIG_VERSION }}.tar.xz
          echo "$(pwd)/zig-linux-x86_64-${ZIG_VERSION}" >> $GITHUB_PATH
          zig version # Verify Zig is in PATH

      - name: Run build
        run: zig build

      - name: Build tests
        run: zig test ${{ env.TEST_ROOT_SRC }}
